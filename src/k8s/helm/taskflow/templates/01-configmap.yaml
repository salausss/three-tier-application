apiVersion: v1
kind: ConfigMap
metadata:
  name: db
  namespace: app
  labels:
    app: taskflow
data:
  # Backend env
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "taskflow"
  DB_USER: "taskflow"
  PORT: "3000"
  NODE_ENV: "production"
